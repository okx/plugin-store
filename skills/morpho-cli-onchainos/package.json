{
  "name": "morpho-cli-onchainos",
  "version": "0.1.0",
  "description": "Drive the Morpho lending protocol from the terminal via `npx @morpho-org/cli@latest` — queries vaults/markets/positions and prepares unsigned Morpho transactions with built-in simulation across all supported chains.",
  "type": "module",
  "main": "cli.ts",
  "bin": {
    "morpho-cli-onchainos": "./cli.ts"
  },
  "scripts": {
    "start": "tsx cli.ts",
    "tool": "tsx cli.ts",
    "version-check": "tsx cli.ts --version",
    "help": "tsx cli.ts --help",
    "smoke": "tsx cli.ts --version && tsx cli.ts --help"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.10.0"
  },
  "dependencies": {}
}
